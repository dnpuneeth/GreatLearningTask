<div class="cmtform">
  <% if comment.comment%>
    <% usr = User.find(comment.user_id)%>
    <p class="uname"><%= usr.name.capitalize %></p>
    <p class="cmt"><%= comment.comment%>  
      <%= link_to '..More', user_question_comment_path(question_id: comment.question.id, id: comment.id) %>
    </p>
    <p class="time"> 
      on <%=comment.created_at.strftime('%b %d, %Y') %> 
    </p>
    <!--
    <% content_tag :button, :type => :submit, :class => :positive do %>
      <%= image_tag("avathar.png")%>
       Save
    <% end %>
    -->
  <% end %>
</div>
<br >